//=======================
// Base
//=======================
// Add whichever you need or, preferably, just copy the code from those files. Especially the breakpoints and helper variables
// @require 'base/reset'
@require 'base/variables'
@require 'base/helpers'
@require 'base/colors'
// @require 'base/layout'
// @require 'base/unicode'
@require 'base/fonts'

//=======================
// Fonts
//=======================
// See base/fonts for full listing
// If you need set-font, make sure to include the base/fonts file
// Note all projects require Benton Bold and Miller-Regular so don't change em

// Just set these classes in the HTML
// set-font('benton-bold') /* Graphic headlines */
// set-font('benton-regular')  Graphic subheads
// set-font('bentonsanscond-regular') /* Graphic byline and source */
*,
*::before,
*::after {
  box-sizing: border-box;
}

#line-chart
  height 550px

  .overlay
    fill none
    pointer-events all

  .line-historical_total
    fill none
    stroke orange
    stroke-width 2

  .line-wrapper-historical_total
    .line-point
      fill orange !important

  .line-current_total
    stroke $globe-link
    stroke-width 3
    fill none

  .line-wrapper-current_total
    .line-point
      fill $globe-link !important

  .line-current-total-fill
    fill $globe-link
    fill-opacity 0.2
  //   fill-opacity .2

  .hoverCircle
    stroke #fff
    stroke-width 4

  .circle-historical_total
    fill orange
    stroke #fff

  .circle-current_total
    fill $globe-link
    stroke #fff


